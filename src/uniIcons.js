import Unicon from "vue-unicons";
import {
  uniBrightness,
  uniMoon,
  uniBars,
  uniTrash,
  uniPen,
  uniSignout,
  uniPlus,
  uniCheckCircle,
  uniCircle,
  uniTimesSquare,
} from "vue-unicons/dist/icons";

const usedIcons = [
  uniBrightness,
  uniMoon,
  uniBars,
  uniTrash,
  uniPen,
  uniSignout,
  uniPlus,
  uniCheckCircle,
  uniCircle,
  uniTimesSquare,
];

Unicon.add(usedIcons);

export default Unicon;
